import { Injectable } from '@nestjs/common';

@Injectable()
export class CartService {
  // Cart is handled on the frontend, but we can add server-side cart management here if needed
  // For now, this is a placeholder
}
